int a;
char b;

int main(){
    write("Enter an upper bound for loop: ");
    read(a);
    b = 1;
    while b <= a do
    begin
        write(b); write(" ");
        b = b + 1;
    end;
    newline;
}
